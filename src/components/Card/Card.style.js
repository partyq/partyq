import { StyleSheet } from 'react-native';

export default (theme) => StyleSheet.create({
  // img
  image: {
    height: '100%',
    width: '100%',
  },
  // card
  cardView: {
    margin: theme.fonts.medium,
  },
});
