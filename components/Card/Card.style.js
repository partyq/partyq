import { StyleSheet } from 'react-native'
import theme from '../../assets/style/theme.style'

export default StyleSheet.create({
    // img
    image: {
        height: '100%',
        width: '100%'
    },

    // card
    cardView: {
        margin: theme.SPACE_MEDIUM
    }
});